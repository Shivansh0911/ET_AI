# Origin of the deployed backend, no trailing slash. Vite bakes this in at build time,
# so changing it requires a rebuild — setting it in the host UI alone does nothing.
VITE_API_URL=https://et-ai-vs97.onrender.com
