# Runtime dependencies for the deployed API.
# Versions carry wheels for Python 3.11 (Render) through 3.13 (local dev). The previous
# pydantic==2.7.0 pin had no 3.12+ wheel, which is what forced the PYTHON_VERSION workaround
# in DEPLOYMENT.md.
fastapi==0.135.3
uvicorn==0.44.0
pydantic==2.11.7
groq==1.5.0
tavily-python==0.7.26
python-dotenv==1.2.2

# Inference. scikit-learn is pinned to the version that produced ml/artifacts/detector.joblib —
# unpickling a fitted forest across minor versions is not guaranteed. Training-only
# dependencies (pandas, pyyaml) live in requirements-ml.txt and are not installed on the server.
numpy==2.3.1
scikit-learn==1.9.0
joblib==1.5.3
